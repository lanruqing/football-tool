import { PlayerNameDirective } from './player-name.directive';

describe('PlayerNameDirective', () => {
  it('should create an instance', () => {
    const directive = new PlayerNameDirective();
    expect(directive).toBeTruthy();
  });
});
